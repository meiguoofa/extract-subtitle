# OCR (only needed for --engine ocr)
paddleocr>=3.0.0,<4.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0

# ASR / translation (always needed)
requests>=2.31.0

# TOS object storage (needed for --asr-vendor volc-bigmodel with local files)
tos>=2.7.0

# Utilities
tqdm>=4.66.0
rapidfuzz>=3.6.0

# Web frontend (needed for webapp.py)
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.6
sse-starlette>=1.0.0
aiosqlite>=0.20.0
